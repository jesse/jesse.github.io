---
title: "Launching the blog"
date: "2026-04-09T110000"
tags:
  - blog
  - go
draft: false
---

I wanted a place for longer notes that can live next to the project pages already on this domain, so this blog now shares the same simple GitHub Pages setup.

> The design goal was straightforward: keep prose narrow, make code pleasant to read, and stay out of the way when the writing is doing the real work.

Inline code like `make build` should feel distinct without pulling attention away from the paragraph around it.

```go
package main

import "fmt"

func main() {
    fmt.Println("The blog is live.")
}
```

```
This untagged block stays plain on purpose.
It is still scrollable when the lines are long.
```

```go
fmt.Println("This is a deliberately long line to confirm the code container can grow wider than the prose column before horizontal scrolling has to take over on smaller screens.")
```

Here is the first local image in the post:

![A simple placeholder image](sample-local.png)

That is enough surface area to verify typography, blockquotes, code, and responsive images without adding any client-side scripting at all.
